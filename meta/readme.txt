When `./scripts/build.sh` or `./scripts/sync.sh` are executed everything here is copied to output/

To add mod configs, make a folder here called `config` and put all mod configs there.
You can do the same for `resourcepacks`, `shaderpacks`, and any other folder that is put in `.minecraft/` for a modpack.
